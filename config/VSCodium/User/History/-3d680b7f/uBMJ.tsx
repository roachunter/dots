import type { StageState } from "../model/stage";

type Props = {
  state: StageState;
};

const StageState = ({ state }: Props) => {
  return <div>
    <img src="" alt="" />
  </div>;
};
export default StageState;
